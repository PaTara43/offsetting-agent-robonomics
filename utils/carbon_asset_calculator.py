import datetime


def get_last_burn_date(address: str) -> datetime.date:

    return datetime.date(2022, 8, 23)


def get_kwt_to_burn(address: str, n_current: float) -> float:

    return 1.0

def get_tokens_to_burn(kwt: float, geo: str) -> float:

    return 1.0